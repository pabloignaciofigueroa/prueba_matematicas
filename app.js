window.__resources = {
  fuente3D: 'assets/fredoka.typeface.json',
  caraMoneda: 'assets/colegio-monte-verde.jpg'
};
document.querySelector('script[type="text/x-dc"]').textContent = "class Component extends DCLogic {\n  state = { vista: 'intro', listo: false, ej: 0, paso: 0, estrellas: [], fb: null, elegidos: [], drag: null, x: 0, y: 0, malas: 0 };\n\n  escenaRef = React.createRef();\n  monedaRef = React.createRef();\n  cajaRef = React.createRef();\n  JUEGOS = [];\n\n  // ---------- ronda aleatoria ----------\n  cuantas() {\n    const v = parseInt(this.props.preguntasPorJuego, 10);\n    return Math.max(3, Math.min(6, isNaN(v) ? 4 : v));\n  }\n  mezclar(a) {\n    const c = (a || []).slice();\n    for (let i = c.length - 1; i > 0; i--) { const j = (Math.random() * (i + 1)) | 0; const t = c[i]; c[i] = c[j]; c[j] = t; }\n    return c;\n  }\n  tomar(a, n) { return this.mezclar(a).slice(0, n); }\n\n  construirRonda() {\n    const B = window.BANCO_MAT;\n    if (!B) return false;\n    const n = this.cuantas(), mz = (a) => this.mezclar(a);\n    const ec = (p) => ({ eq: p.eq, r: p.r, ops: mz(p.ops) });\n    this.JUEGOS = [\n      { nombre: 'Cajitas del 10', color: '#00E5FF', tipo: 'drag', instr: 'Arrastra la ficha a la cajita',\n        pasos: this.tomar(B.cajitas10, n).map(ec) },\n      { nombre: 'Familias del 10', color: '#C6FF00', tipo: 'ec', instr: 'Toca el número que falta',\n        pasos: this.tomar(B.familias10, n).map(ec) },\n      { nombre: 'Series', color: '#FF7A00', tipo: 'serie', instr: '¿Qué número falta?',\n        pasos: this.tomar(B.series, n).map((p) => ({ cel: p.cel, r: p.r, ops: mz(p.ops) })) },\n      { nombre: 'Mayor a menor', color: '#FF2DBE', tipo: 'orden', instr: 'Toca del más grande al más chico',\n        nums: mz((this.tomar(B.orden, 1)[0] || { nums: [8, 19, 30, 41, 45, 49] }).nums) },\n      { nombre: '¿Cuántos hay?', color: '#FFE500', tipo: 'conteo', instr: 'Cuenta y toca el número',\n        pasos: this.tomar(B.conteo, Math.min(n, 4)).map((p) => ({ n: p.n, forma: p.forma, r: p.r, ops: mz(p.ops) })) },\n      { nombre: 'Restas', color: '#00E5FF', tipo: 'ec', instr: 'Toca el resultado',\n        pasos: this.tomar(B.restas, n + 1).map(ec) }\n    ];\n    return true;\n  }\n  esperarBanco() {\n    if (this.construirRonda()) { this.forceUpdate(); return; }\n    this._banco = setTimeout(() => this.esperarBanco(), 120);\n  }\n\n  // ---------- ciclo de vida ----------\n  componentDidMount() {\n    this.esperarBanco();\n    if (this.props.mostrarIntro === false) this.setState({ vista: 'juego', listo: true });\n    else {\n      this._to = setTimeout(() => this.setState({ listo: true }), 2500);\n      this.iniciarIntro();\n    }\n    this._mv = (e) => { if (this.state.drag) { if (e.cancelable) e.preventDefault(); this.setState({ x: e.clientX, y: e.clientY }); } };\n    this._up = () => {\n      const d = this.state.drag;\n      if (!d) return;\n      const c = this.cajaRef.current, r = c && c.getBoundingClientRect();\n      const dentro = r && this.state.x > r.left - 50 && this.state.x < r.right + 50 && this.state.y > r.top - 50 && this.state.y < r.bottom + 50;\n      this.setState({ drag: null });\n      if (dentro) this.responder(d.v);\n    };\n    document.addEventListener('pointermove', this._mv, { passive: false });\n    document.addEventListener('pointerup', this._up);\n  }\n  componentWillUnmount() {\n    clearTimeout(this._to); clearTimeout(this._auto); clearTimeout(this._banco); clearTimeout(this._intro3d);\n    this.detenerIntro();\n    document.removeEventListener('pointermove', this._mv);\n    document.removeEventListener('pointerup', this._up);\n  }\n\n  // ---------- intro 3D ----------\n  iniciarIntro(intentos) {\n    if (this.state.vista !== 'intro') return;\n    const cont = this.escenaRef.current, T = window.THREE;\n    if (!window.WebGLRenderingContext) { this.setState({ listo: true }); return; }\n    if (!cont || !T) { // three.js puede cargar después del montaje (archivo standalone)\n      if ((intentos || 0) > 80) { this.setState({ listo: true }); return; }\n      this._intro3d = setTimeout(() => this.iniciarIntro((intentos || 0) + 1), 100);\n      return;\n    }\n\n    const FLUOR = [0xFF0055, 0xFF7A00, 0xFFE500, 0x66FF00, 0x00E5FF, 0x0033FF, 0x7B00FF, 0xFF00D9, 0xFF2D78];\n    const INK = 0x171528;\n    const ancho = () => cont.clientWidth || window.innerWidth;\n    const alto = () => cont.clientHeight || window.innerHeight;\n\n    const scene = new T.Scene();\n    const camera = new T.PerspectiveCamera(40, ancho() / alto(), 0.1, 100);\n    camera.position.set(0, 0, Math.max(11, Math.min(26, 11.5 / (ancho() / alto()))));\n    let renderer;\n    try { renderer = new T.WebGLRenderer({ antialias: true, alpha: true }); }\n    catch (e) { this.setState({ listo: true }); return; }\n    renderer.setSize(ancho(), alto());\n    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n    renderer.outputEncoding = T.sRGBEncoding;\n    cont.appendChild(renderer.domElement);\n    const fallbackIntro = cont.parentElement && cont.parentElement.querySelector('.intro-fallback');\n    if (fallbackIntro) fallbackIntro.style.opacity = '0';\n\n    scene.add(new T.AmbientLight(0xffffff, 0.75));\n    const l1 = new T.DirectionalLight(0xffffff, 0.85); l1.position.set(4, 6, 8); scene.add(l1);\n    const l2 = new T.DirectionalLight(0xffeecc, 0.35); l2.position.set(-5, -2, 4); scene.add(l2);\n    const grupo = new T.Group(); scene.add(grupo);\n\n    // moneda del colegio\n    let ejeMoneda = null, rMoneda = null, sMoneda = null, cMoneda = null;\n    const elM = this.monedaRef.current;\n    if (elM) {\n      rMoneda = new T.WebGLRenderer({ antialias: true, alpha: true });\n      rMoneda.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n      rMoneda.outputEncoding = T.sRGBEncoding;\n      elM.appendChild(rMoneda.domElement);\n      rMoneda.setSize(elM.clientWidth || 110, elM.clientHeight || 110);\n      sMoneda = new T.Scene();\n      cMoneda = new T.PerspectiveCamera(35, 1, 0.1, 20);\n      cMoneda.position.set(0, 0, 3.4);\n      sMoneda.add(new T.AmbientLight(0xffffff, 0.6));\n      const lm = new T.DirectionalLight(0xffffff, 1.0); lm.position.set(2, 3, 4); sMoneda.add(lm);\n      const canto = () => {\n        const c = document.createElement('canvas'); c.width = 16; c.height = 64;\n        const g = c.getContext('2d');\n        const grad = g.createLinearGradient(0, 0, 0, 64);\n        grad.addColorStop(0, '#f7f1c0'); grad.addColorStop(0.5, '#ddd3a0'); grad.addColorStop(1, '#f7f1c0');\n        g.fillStyle = grad; g.fillRect(0, 0, 16, 64);\n        const t = new T.CanvasTexture(c); t.encoding = T.sRGBEncoding; return t;\n      };\n      const R = window.__resources || {};\n      new T.TextureLoader().load(R.caraMoneda || 'cara.jpg', (tex) => {\n        tex.encoding = T.sRGBEncoding;\n        const texR = tex.clone(); texR.needsUpdate = true;\n        texR.center.set(0.5, 0.5); texR.rotation = Math.PI;\n        const matF = new T.MeshStandardMaterial({ map: tex, metalness: 0.15, roughness: 0.42 });\n        const matR = new T.MeshStandardMaterial({ map: texR, metalness: 0.15, roughness: 0.42 });\n        const matC = new T.MeshStandardMaterial({ map: canto(), metalness: 0.3, roughness: 0.45 });\n        const m = new T.Mesh(new T.CylinderGeometry(1, 1, 0.14, 96), [matC, matF, matR]);\n        m.rotation.z = Math.PI / 2;\n        ejeMoneda = new T.Group(); ejeMoneda.add(m); ejeMoneda.rotation.x = 0.12;\n        sMoneda.add(ejeMoneda);\n      });\n    }\n\n    // partículas\n    const particulas = [], geoPart = new T.BoxGeometry(0.09, 0.09, 0.09);\n    const estallido = (pos, color) => {\n      for (let i = 0; i < 18; i++) {\n        const m = new T.Mesh(geoPart, new T.MeshBasicMaterial({\n          color: Math.random() < 0.5 ? color : FLUOR[(Math.random() * FLUOR.length) | 0], transparent: true\n        }));\n        m.position.copy(pos);\n        const a = Math.random() * Math.PI * 2, v = 2.5 + Math.random() * 3.5;\n        m.userData = { vx: Math.cos(a) * v, vy: Math.sin(a) * v + 2, vz: (Math.random() - 0.5) * 2,\n          rx: (Math.random() - 0.5) * 8, rz: (Math.random() - 0.5) * 8, vida: 1 };\n        scene.add(m); particulas.push(m);\n      }\n    };\n\n    // letras 3D\n    const letras = [], flotantes = [];\n    const raycaster = new T.Raycaster(), puntero = new T.Vector2(0, 0);\n    const _cA = new T.Color(), _cB = new T.Color(INK), _tmpV = new T.Vector3();\n    let letraHover = null;\n\n    const linea = (font, texto, size, y, off) => {\n      const altura = size * 0.34, geoms = [];\n      let cursor = 0;\n      for (const ch of texto) {\n        const g = new T.TextGeometry(ch, { font, size, height: altura, curveSegments: 10,\n          bevelEnabled: true, bevelThickness: size * 0.035, bevelSize: size * 0.022, bevelSegments: 3 });\n        g.computeBoundingBox();\n        const w = g.boundingBox.max.x - g.boundingBox.min.x;\n        geoms.push({ g, w }); cursor += w + size * 0.14;\n      }\n      cursor -= size * 0.14;\n      let x = -cursor / 2;\n      geoms.forEach((it, i) => {\n        const color = FLUOR[(i + off) % FLUOR.length];\n        const m = new T.Mesh(it.g, new T.MeshStandardMaterial({ color, roughness: 0.35, metalness: 0.05 }));\n        m.position.set(x, y, 0);\n        m.userData = { baseY: y, fase: Math.random() * Math.PI * 2, indice: letras.length, color,\n          tinteT: 0, escalaObj: 0, reboteY: 0, velY: 0 };\n        m.scale.setScalar(0);\n        grupo.add(m); letras.push(m);\n        x += it.w + size * 0.14;\n      });\n    };\n    const fondo = (font) => {\n      const simbolos = ['+', '−', '×', '÷', '=', '1', '2', '3', '4', '5', '7', '9', '?', '!'];\n      for (let i = 0; i < 20; i++) {\n        const size = 0.3 + Math.random() * 0.5;\n        const g = new T.TextGeometry(simbolos[i % simbolos.length], { font, size, height: size * 0.22, curveSegments: 5 });\n        const m = new T.Mesh(g, new T.MeshBasicMaterial({ color: FLUOR[i % FLUOR.length], transparent: true, opacity: 0.28 + Math.random() * 0.2 }));\n        m.position.set((Math.random() - 0.5) * 17, (Math.random() - 0.5) * 10, -3 - Math.random() * 4);\n        m.rotation.set(Math.random() * 0.6 - 0.3, Math.random() * 0.6 - 0.3, Math.random() * 0.6 - 0.3);\n        m.userData = { vy: 0.15 + Math.random() * 0.3, fase: Math.random() * 6.28, rz: (Math.random() - 0.5) * 0.5 };\n        scene.add(m); flotantes.push(m);\n      }\n    };\n\n    let t = 0, t0 = null;\n    const reloj = new T.Clock();\n    const RF = window.__resources || {};\n    new T.FontLoader().load(RF.fuente3D || 'fredoka.typeface.json', (font) => {\n      window.__mvFuenteOK = true;\n      linea(font, 'REPASO', 1.6, 1.1, 0);\n      linea(font, 'MATEMATICAS', ancho() / alto() < 0.8 ? 0.64 : 0.92, -1.15, 4);\n      fondo(font);\n      t = 0; t0 = null; reloj.getDelta();\n    }, undefined, () => this.setState({ listo: true }));\n\n    this._setPuntero = (e) => {\n      puntero.x = (e.clientX / ancho()) * 2 - 1;\n      puntero.y = -(e.clientY / alto()) * 2 + 1;\n    };\n    this._tocar = (e) => {\n      if (this.state.vista !== 'intro') return;\n      this._setPuntero(e);\n      raycaster.setFromCamera(puntero, camera);\n      const hit = raycaster.intersectObjects(letras, false)[0];\n      if (hit) {\n        hit.object.userData.velY = 6.5;\n        hit.object.getWorldPosition(_tmpV);\n        estallido(_tmpV, hit.object.userData.color);\n      }\n    };\n    this._resize = () => {\n      camera.aspect = ancho() / alto();\n      camera.position.z = Math.max(11, Math.min(26, 11.5 / camera.aspect));\n      camera.updateProjectionMatrix();\n      renderer.setSize(ancho(), alto());\n      if (rMoneda && elM) rMoneda.setSize(elM.clientWidth || 110, elM.clientHeight || 110);\n    };\n    window.addEventListener('pointermove', this._setPuntero);\n    window.addEventListener('pointerdown', this._tocar);\n    window.addEventListener('resize', this._resize);\n\n    this._intro = { renderer, rMoneda };\n    this._vivo = true;\n    const animar = () => {\n      if (!this._vivo) return;\n      this._raf = requestAnimationFrame(animar);\n      const dt = Math.min(reloj.getDelta(), 0.05);\n      if (t0 === null) t0 = performance.now();\n      t = (performance.now() - t0) / 1000;\n\n      raycaster.setFromCamera(puntero, camera);\n      const hit = raycaster.intersectObjects(letras, false)[0];\n      letraHover = hit ? hit.object : null;\n\n      letras.forEach((m) => {\n        const u = m.userData, retraso = u.indice * 0.09 + 0.15;\n        let objetivo = t > retraso ? 1 : 0;\n        if (m === letraHover) objetivo = 1.22;\n        u.escalaObj += (objetivo - u.escalaObj) * 0.22;\n        const rebote = 1 + Math.sin(Math.min(Math.max((t - retraso) * 6, 0), Math.PI)) * 0.12;\n        m.scale.setScalar(Math.max(u.escalaObj * rebote, 0.0001));\n        const objT = (m === letraHover) ? 0.35 : 0;\n        u.tinteT += (objT - u.tinteT) * 0.15;\n        _cA.setHex(u.color);\n        m.material.color.copy(_cA).lerp(_cB, u.tinteT);\n        m.position.y = u.baseY + u.reboteY + Math.sin(t * 2 + u.fase) * 0.07;\n        m.position.z = (m === letraHover) ? 0.45 : 0;\n        m.rotation.z = Math.sin(t * 1.6 + u.fase) * 0.03;\n        if (u.velY !== 0 || u.reboteY > 0) {\n          u.reboteY += u.velY * dt; u.velY -= 22 * dt;\n          if (u.reboteY <= 0) { u.reboteY = 0; u.velY = 0; }\n        }\n      });\n\n      flotantes.forEach((m) => {\n        m.position.y += m.userData.vy * dt;\n        m.rotation.z += m.userData.rz * dt;\n        m.position.x += Math.sin(t * 0.8 + m.userData.fase) * 0.0015;\n        if (m.position.y > 5.5) m.position.y = -5.5;\n      });\n\n      for (let i = particulas.length - 1; i >= 0; i--) {\n        const p = particulas[i], u = p.userData;\n        p.position.x += u.vx * dt; p.position.y += u.vy * dt; p.position.z += u.vz * dt;\n        u.vy -= 9 * dt;\n        p.rotation.x += u.rx * dt; p.rotation.z += u.rz * dt;\n        u.vida -= dt * 1.4;\n        p.material.opacity = Math.max(u.vida, 0);\n        p.scale.setScalar(Math.max(u.vida, 0.01));\n        if (u.vida <= 0) { scene.remove(p); p.material.dispose(); particulas.splice(i, 1); }\n      }\n\n      camera.position.x += ((puntero.x * 0.7) - camera.position.x) * 0.04;\n      camera.position.y += ((puntero.y * 0.45) - camera.position.y) * 0.04;\n      camera.lookAt(0, 0, 0);\n      renderer.render(scene, camera);\n\n      if (ejeMoneda && rMoneda) {\n        ejeMoneda.rotation.y = t * 0.9 - Math.PI / 2;\n        rMoneda.render(sMoneda, cMoneda);\n      }\n    };\n    animar();\n  }\n  detenerIntro() {\n    this._vivo = false;\n    clearTimeout(this._intro3d);\n    cancelAnimationFrame(this._raf);\n    window.removeEventListener('pointermove', this._setPuntero);\n    window.removeEventListener('pointerdown', this._tocar);\n    window.removeEventListener('resize', this._resize);\n    if (this._intro) {\n      try { this._intro.renderer.dispose(); if (this._intro.rMoneda) this._intro.rMoneda.dispose(); } catch (e) {}\n      this._intro = null;\n    }\n  }\n\n  // ---------- juego ----------\n  comenzar = () => {\n    clearTimeout(this._to);\n    this.detenerIntro();\n    if (!this.JUEGOS.length) this.construirRonda();\n    this.setState({ vista: 'juego', ej: 0, paso: 0, fb: null, elegidos: [], malas: 0 });\n  };\n  jugarOtraVez = () => {\n    clearTimeout(this._auto);\n    this.construirRonda();\n    this.setState({ vista: 'juego', ej: 0, paso: 0, estrellas: [], fb: null, elegidos: [], malas: 0 });\n  };\n\n  juego() { return this.JUEGOS[this.state.ej] || { nombre: '', color: '#C6FF00', tipo: '', instr: 'Preparando…', pasos: [] }; }\n  pasoActual() { const j = this.juego(); return j.pasos ? j.pasos[this.state.paso] : null; }\n\n  abrir(i) { this.setState({ vista: 'juego', ej: i, paso: 0, fb: null, elegidos: [], malas: 0 }); }\n\n  cerrarPremio = () => {\n    clearTimeout(this._auto);\n    if (this.state.estrellas.length >= this.JUEGOS.length) { this.setState({ vista: 'final' }); return; }\n    let sig = -1;\n    for (let k = 1; k <= this.JUEGOS.length; k++) {\n      const i = (this.state.ej + k) % this.JUEGOS.length;\n      if (this.state.estrellas.indexOf(i) < 0) { sig = i; break; }\n    }\n    if (sig < 0) { this.setState({ vista: 'final' }); return; }\n    this.setState({ vista: 'juego', ej: sig, paso: 0, fb: null, elegidos: [], malas: 0 });\n  };\n\n  premiar() {\n    const e = this.state.estrellas.slice();\n    if (e.indexOf(this.state.ej) < 0) e.push(this.state.ej);\n    this.setState({ estrellas: e, vista: 'premio', fb: null });\n    clearTimeout(this._auto);\n    this._auto = setTimeout(this.cerrarPremio, 2600);\n  }\n\n  responder(v) {\n    const j = this.juego();\n    if (j.tipo === 'orden') return this.responderOrden(v);\n    const p = this.pasoActual();\n    if (!p) return;\n    if (v !== p.r) { this.setState({ fb: 'mal', malas: this.state.malas + 1 }); return; }\n    this.setState({ fb: 'bien' });\n    setTimeout(() => {\n      if (this.state.paso + 1 < j.pasos.length) this.setState({ paso: this.state.paso + 1, fb: null });\n      else this.premiar();\n    }, 750);\n  }\n\n  responderOrden(v) {\n    const j = this.juego();\n    const restan = j.nums.filter((n) => this.state.elegidos.indexOf(n) < 0);\n    const mayor = Math.max.apply(null, restan);\n    if (v !== mayor) { this.setState({ fb: 'mal', malas: this.state.malas + 1 }); return; }\n    const el = this.state.elegidos.concat([v]);\n    this.setState({ elegidos: el, fb: 'bien' });\n    if (el.length === j.nums.length) setTimeout(() => this.premiar(), 700);\n    else setTimeout(() => this.setState({ fb: null }), 350);\n  }\n\n  renderVals() {\n    const st = this.state, j = this.juego(), p = this.pasoActual();\n    const acento = this.props.colorAcento || '#C6FF00';\n    const vals = {\n      verIntro: st.vista === 'intro',\n      cargando: st.vista === 'intro' && !st.listo,\n      listo: st.vista === 'intro' && st.listo,\n      comenzar: this.comenzar,\n      jugarOtraVez: this.jugarOtraVez,\n      escenaRef: this.escenaRef,\n      monedaRef: this.monedaRef,\n      verPremio: st.vista === 'premio',\n      verFinal: st.vista === 'final',\n      totalEstrellas: st.estrellas.length,\n      cerrarPremio: this.cerrarPremio,\n      tarjetas: this.JUEGOS.map((g, i) => {\n        const hecho = st.estrellas.indexOf(i) >= 0, activo = i === st.ej;\n        return { nombre: g.nombre, estrella: hecho ? '★' : '☆',\n          fondo: activo ? g.color : '#fffdf5',\n          borde: activo ? '#171528' : 'rgba(23,21,40,.25)',\n          estrellaColor: hecho ? '#FFB800' : 'rgba(23,21,40,.3)',\n          onClick: () => this.abrir(i) };\n      }),\n      cajaRef: this.cajaRef,\n      arrastrando: !!st.drag,\n      dragVal: st.drag ? st.drag.v : '',\n      dragX: st.x + 'px', dragY: st.y + 'px',\n      instruccion: j.instr,\n      esEcuacion: j.tipo === 'ec',\n      esDrag: j.tipo === 'drag',\n      esSerie: j.tipo === 'serie',\n      esConteo: j.tipo === 'conteo',\n      esOrden: j.tipo === 'orden',\n      cajaColor: st.fb === 'bien' ? '#C6FF00' : '#00E5FF',\n      enCaja: st.fb === 'bien' && p ? p.r : '?',\n      enunciado: p ? p.eq : '',\n      celdas: [], objetos: [], elegidos: [],\n      textoPremio: st.malas > 0\n        ? 'Te equivocaste y no importa: seguiste intentando y la ganaste.'\n        : '¡Lo hiciste todo tú!'\n    };\n\n    const pasosN = j.pasos ? j.pasos.length : (j.nums ? j.nums.length : 0);\n    const hechos = j.pasos ? st.paso : st.elegidos.length;\n    vals.puntos = Array.from({ length: pasosN }, (_, i) => ({ color: i < hechos ? acento : '#fffdf5' }));\n\n    if (st.fb === 'mal') { vals.aviso = '¡Casi! Prueba otra vez, tu ★ te espera.'; vals.avisoFondo = '#ffeaf9'; }\n    else if (st.fb === 'bien') { vals.aviso = '¡Muy bien!'; vals.avisoFondo = '#eeffc4'; }\n    else { vals.aviso = 'Puedes intentar todas las veces que quieras.'; vals.avisoFondo = '#fff9d6'; }\n\n    if (j.tipo === 'serie' && p) {\n      vals.celdas = p.cel.map((v) => v === null\n        ? { texto: '?', bg: '#FFE500', borde: '#171528' }\n        : { texto: String(v), bg: '#fffdf5', borde: 'rgba(23,21,40,.3)' });\n    }\n\n    if (j.tipo === 'conteo' && p) {\n      const huevo = p.forma === 'huevo';\n      vals.objetos = Array.from({ length: p.n }, () => huevo\n        ? { alto: '104px', radio: '50% 50% 46% 46% / 60% 60% 40% 40%', fondo: '#fffdf5', glifo: '' }\n        : { alto: '56px', radio: '10px', fondo: '#C6FF00', glifo: '$' });\n    }\n\n    let ops = [];\n    if (j.tipo === 'orden') {\n      vals.elegidos = st.elegidos.map((n) => ({ n }));\n      ops = j.nums.filter((n) => st.elegidos.indexOf(n) < 0).map((n) => ({\n        n, ancho: '160px', fondo: '#fffdf5', anim: st.fb === 'mal' ? 'sacude .4s' : 'none',\n        touchAction: 'manipulation', claseTactil: '', onClick: () => this.responder(n), onDown: null\n      }));\n    } else if (j.tipo === 'drag' && p) {\n      ops = p.ops.map((n) => ({\n        n, ancho: '130px', fondo: '#00E5FF', anim: 'none', touchAction: 'none', claseTactil: 'game-drag-option', onClick: null,\n        onDown: (e) => this.setState({ drag: { v: n }, x: e.clientX, y: e.clientY })\n      }));\n    } else if (p) {\n      ops = p.ops.map((n) => ({\n        n, ancho: '230px', fondo: j.color, anim: st.fb === 'mal' ? 'sacude .4s' : 'none',\n        touchAction: 'manipulation', claseTactil: '', onClick: () => this.responder(n), onDown: null\n      }));\n    }\n    vals.opciones = ops;\n    return vals;\n  }\n}";
